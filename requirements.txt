boto3>=1.34.0
e2b-code-interpreter>=1.0.0
python-dotenv>=1.0.0
certifi>=2024.0.0
# Gemini: REST con urllib + certifi (evita SSL: CERTIFICATE_VERIFY_FAILED en macOS).

# tiktoken: opcional (mejor conteo de tokens). Requiere pip reciente; con Python 3.10+:
#   python -m pip install -U pip
#   pip install tiktoken
